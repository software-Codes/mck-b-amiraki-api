const {sql} = require('../../config/database');


class MediaContent {
    static async create({
        title, 
        description, 
        contentType, 
        url, 
        thumbnailUrl, 
        uploadedBy,
        size,
        duration 
    }){
        const query = `
        
        `
    }
}